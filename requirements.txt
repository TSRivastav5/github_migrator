PyGithub==2.3.0
requests==2.31.0
python-dotenv==1.0.0
# tkinter is built into CPython — no pip install required
# cloc must be installed at the OS level:
#   macOS:  brew install cloc
#   Linux:  sudo apt install cloc
